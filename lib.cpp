#include "lib.h"
float  numeroMas{
  simo(const float numeri[], int dim){
  numeri[dim];
  float massimo=numeri[0];
  for (int i=0; i<dim; i++){
    if(massimo<numeri[i];
       }
       }
       return msssimo;
       }
